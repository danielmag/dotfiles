[user]
  name = Daniel M
[push]
  default = current
[core]
  excludesfile = ~/.gitignore
  editor = nvim
[alias]
  undo = "reset --soft HEAD~1"
  safe = "!mkdir -p .git/safe"
[commit]
  template = ~/.gitmessage
[fetch]
  prune = true
